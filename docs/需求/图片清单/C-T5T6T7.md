# C - 场景一 T5/T6/T7 六张界面图 判读清单

- 图片根目录：`D:\dsh_workpath\webMap\参考文档\需求图与描述\场景1\`
- 本次判读图片：`T5-1.png`、`T5-2.png`、`T6-1.png`、`T6-2.png`、`T7-1.png`、`T7-2.png`（6 张，均读成功）
- 判读方式：逐张整图判读＋局部放大（裁剪放大，仅用于看清小字，未改动任何图片）
- 判读纪律：标题/字段/按钮/数值逐字抄录（含全角标点）；小字看不清处标"看不清"，不作推测；对照材料（文字稿、Excel《界面交互逻辑》）仅作对照，**图为准**

## 六屏共同"应用壳"（后文差异对比的基准）

- 顶栏左：三角标 Logo ＋「智能任务管理系统」；顶栏右：「网络：已连接」＋ Wi-Fi 图标 ＋ 电池图标 ＋ 「15:14」＋ 圆形用户头像
- 最左竖向图标导航 7 项（自上而下）：「态势」「任务」「目标」「区域」「资源」「告警」「设置」，本组 6 张图**全部高亮第一项「态势」**
- 地图区左上工具条：「选择」「测距」「图层」「3D」「2D/3D」；地图区右上胶囊下拉：「显示模式：×××× ▾」
- 地图区右侧：T5-1 无缩放控件（仅绿色多边形）；T5-2/T6-1/T6-2/T7-1/T7-2 有「N」指北针 ＋「+」「−」
- 最底部全局状态条 6 段（6 张图逐字相同）：「系统状态：运行正常」「无人机状态：全部正常」「数据链路：稳定」「定位状态：正常」「AI决策引擎：在线」「安全状态：安全」
- 底图：全部为深色 3D 地形/山体起伏（带 3D 2D/3D 切换），无视频窗口、无比例尺
- 6 张图各自「显示模式」取值：T5-1「任务规划」／T5-2「打击确认」／T6-1「实时态势」／T6-2「引导控制」／T7-1「复核态势」／T7-2「结果汇总」

---

## T5-1.png —— AI 决策界面（任务决策与打击准备）

- 整体版式：顶栏 ＋ 最左竖向图标导航 ＋ 中央大区（上约 2/3 为 3D 地图、下约 1/3 为「打击方案」3 卡横排）＋ 右侧「AI语音交互」整栏；最底部一条全局状态条（贯穿全宽）。
- 顶栏文字（逐字）：「智能任务管理系统」（左，三角标 Logo）；右侧「网络：已连接」「15:14」；左导航文字「态势」「任务」「目标」「区域」「资源」「告警」「设置」。
- 左侧面板：无独立左侧面板（仅竖向图标导航条）。地图区工具条文字：「选择」「测距」「图层」「3D」「2D/3D」；地图右上「显示模式：任务规划 ▾」。
- 中央区域：3D 地形地图（山地＋城镇）；中央为红色同心圆目标标记（红色十字准星），目标外围一圈半透明青色圆形高亮（覆盖城镇），周边 6 个蓝色四旋翼无人机图标以浅蓝色**虚线**与目标相连（无箭头）；右侧一片绿色多边形区域；地图上**无任何文字标签**（无目标编号）。地图下方标题「打击方案」＋ 3 张卡片横排（左→右）：①「方案一：光电精确打击」（准星图标）：打击方式：精确打击；参与集群：集群2（光电）；成功率预估：68%；打击效果：精准摧毁。②「方案二：多集群协同压制」（无人机四向箭头图标）：**卡片蓝色描边高亮**，卡片右上绿色角标「推荐方案」；打击方式：协同压制；参与集群：集群1/2/3/4/5/6；成功率预估：82%；打击效果：压制摧毁。③「方案三：电子干扰配合」（紫色信号塔图标）：打击方式：电子干扰+打击；参与集群：集群4（电子）+集群5（通信）；成功率预估：61%；打击效果：干扰瘫痪后打击。
- 右侧面板：标题「AI语音交互」；①「系统说：」（右侧绿色声波图标）「基于目标价值、暴露时间窗口与链路稳定性，已生成三种打击方案。系统可同步生成目标信息包并推送至后方远程火力与空中支援力量。」②「用户说：」（麦克风图标）「“选择最优方案”」③「系统回复：」「已为你选择方案二：多集群协同压制，该方案在当前环境下综合成功率最高。建议由无人机持续跟踪目标，并为远程火力提供实时修正信息。」；面板底部一枚大号圆形蓝色麦克风按钮。
- 底部：无按钮；最下方为全局状态条 6 段（「系统状态：运行正常」「无人机状态：全部正常」「数据链路：稳定」「定位状态：正常」「AI决策引擎：在线」「安全状态：安全」）。
- 配色与图形：深色近黑底＋亮蓝（主色）／青色；推荐角标为绿色，目标与目标连线为红色；无人机为蓝色线框四旋翼图标，连线为浅蓝虚线（**无箭头**）；目标为红色同心圆＋十字准星；绿色多边形为区域面；有 3D 地形；**无视频窗口**。
- 图上出现的具体数值：成功率预估 68% ／ 82% ／ 61%；参与集群 集群2（光电）、集群1/2/3/4/5/6、集群4（电子）+集群5（通信）；顶栏时间 15:14。
- 与文字稿/Excel 描述**不一致**或**图上没有**的地方：
  - Excel：「卡片栏右上方有‘确认’按钮」——**图上未见**任何「确认」按钮（卡片栏右上方区域没有按钮）。
  - Excel：「地图高亮显示分配该方案的无人机 ＋ 与打击目标间保持高亮引导连线」——**图上未见**按方案区分的无人机高亮或按方案区别的引导连线（6 架机与目标的虚线连线不做方案区分），疑似描述有误。
  - Excel：「用户语音‘选择最优方案’→跳转打击方案确认界面」——图上只有语音气泡「“选择最优方案”」与「系统回复」，**图上未见**跳转按钮/跳转提示。
  - 文字稿 AI 语音原文为「基于目标价值、暴露时间窗口与链路稳定性，已生成三种打击方案…」，图上「系统说」为「…与链路稳定性，已生成三种打击方案。系统可同步生成目标信息包并推送至后方远程火力与空中支援力量。」——内容一致，仅截断差异，非矛盾。
  - 图上「方案二」标题下方有一行小字被重复/重叠渲染（两层字压在一起），**看不清**；疑似生成图缺陷，图上未见可辨识副标题。
  - 图上全屏**均无**「t0」「ip 点」字样；也**无**任务时间轴。

---

## T5-2.png —— 打击确认界面（打击方案确认）

- 整体版式：顶栏 ＋ 最左竖向图标导航 ＋ 中央大区（上部约 2/3 为 3D 地图；下部左为「目标清单」卡、中为「打击窗口时间轴」，其下为两个按钮）＋ 右侧 3 张面板（资源调度情况／信息包与协同状态／AI确认建议）；最底部全局状态条。
- 顶栏文字（逐字）：「智能任务管理系统」；「网络：已连接」「15:14」；左导航「态势」「任务」「目标」「区域」「资源」「告警」「设置」。
- 左侧面板：无独立左侧面板（仅竖向导航条）。工具条：「选择」「测距」「图层」「3D」「2D/3D」；地图右上「显示模式：打击确认 ▾」。
- 中央区域：3D 地形地图；目标区红色圆环＋红色锁定标记，目标上方**红色标签框两行**「目标003」「指挥通信节点」；多架蓝色无人机图标与虚线连线；右侧绿色多边形区域内含一枚**绿色箭头（指向左）**（进入/打击方向）；地图右侧「N」「+」「−」。下部：①「目标清单」卡（3 行，行首为菱形编号）：红色菱形「1」＋「目标003 ｜ 指挥通信节点」＋红框标签「高价值」＋「优先级 1」（该行有蓝色高亮描边）；橙色菱形「2」＋「目标001 ｜ 防空火力阵地」＋橙框标签「中高威胁」＋「优先级 2」；黄色菱形「3」＋「目标002 ｜ 通信保障节点」＋黄框标签「中威胁」＋「优先级 3」。②「打击窗口时间轴」（面板右端「窗口剩余 08:42」）：4 段卡片「T+00 信息包生成」（绿色高亮卡）→「T+03 火力协同确认」→「T+06 空中支援接入」→「T+09 等待任务生成」，卡片之间为蓝色箭头（最后一段箭头为灰色）；卡片下方一条节点进度线：T+00 绿色实心节点、T+03 蓝色实心节点、T+06 蓝色实心带环节点、T+09 灰色空心节点，连线由绿色→青色→虚线灰。
- 右侧面板：①「资源调度情况」：「光电集群：」→「持续跟踪」；「雷达集群：」→「位置校正」；「电子集群：」→「压制待命」；「通信集群：」→「链路中继」（4 项右侧均为绿色描边标签）。②「信息包与协同状态」：「目标信息包生成状态」→ 青色进度条约 92% ＋「92%」；「远程火力协同状态」→ 青色满条 ＋「已接入」；「空中支援接入状态」→ 橙色约半条 ＋「待确认」；「链路稳定性」→ 绿色条约 85% ＋「稳定」。③「AI确认建议」：「建议保持方案二：多集群协同压制。目标003仍处于有效暴露窗口，信息包已具备推送条件。」
- 底部：按钮「生成打击任务」（蓝色实心，较宽）、「调整方案」（深色描边）；再下为全局状态条 6 段（同通例逐字）。
- 配色与图形：深色底＋蓝/青主色；威胁等级用红／橙／黄三档描边标签；进度条青／橙／绿三色；地图为浅蓝虚线连线＋绿色面区域＋绿色方向箭头；有 3D 地形；**无视频窗口**。
- 图上出现的具体数值：「窗口剩余 08:42」；时间轴 T+00 ／ T+03 ／ T+06 ／ T+09；「92%」；优先级 1 ／ 2 ／ 3；顶栏 15:14。
- 与文字稿/Excel 描述**不一致**或**图上没有**的地方：
  - Excel：时间轴应显示「任务下发时间 t0」「同步到达 ip 点时间」「打击时间」「评估完成时间」——图上实为「T+00 信息包生成／T+03 火力协同确认／T+06 空中支援接入／T+09 等待任务生成」，**无 t0、无 ip 点、无打击时间、无评估完成时间**，疑似描述有误。
  - Excel：「地图高亮：分配无人机/攻击起点(ip)点/无人机与 ip 点连线/毁伤评估无人机的评估任务航线及其连线」——图上**未见** ip 点、**未见**评估机评估任务航线及其标注，疑似描述有误/图上未见。
  - Excel：右边「协同状态」栏应显示「指控链路状态／蜂群自组网状态／远程火力协同状态」——图上右栏标题为「信息包与协同状态」，条目为「目标信息包生成状态／远程火力协同状态／空中支援接入状态／链路稳定性」，**无「指控链路状态」「蜂群自组网状态」**，疑似描述有误。
  - Excel：「右边‘资源调度情况’栏显示分配该任务的所有无人机状态」——图上只列 4 个**集群级**状态（光电／雷达／电子／通信集群），**无单机（无人机）列表**，疑似描述有误。
  - 文字稿把「目标信息包生成状态」列为一屏要素——图上它位于右栏「信息包与协同状态」内（进度条＋92%），并非独立卡片，位置与文字稿的并列关系不同。
  - Excel：「点击‘生成打击任务’→提示‘任务已下发’」——图上**未见**「任务已下发」提示文本。
  - 图上**无**「48 分钟」等时长数值（本屏唯一时长为窗口剩余 08:42）。

---

## T6-1.png —— 执行态势界面（协同执行与引导）

- 整体版式：顶栏 ＋ 最左竖向图标导航 ＋ 中央大区（3D 地图占满，左下角浮出 3 个状态标签与 2 个按钮）＋ 右侧 3 张面板（AI提示／跟踪状态／协同链路）；最底部全局状态条。
- 顶栏文字（逐字）：「智能任务管理系统」；「网络：已连接」「15:14」；左导航 7 项同通例。
- 左侧面板：无独立左侧面板（仅竖向导航条）。工具条：「选择」「测距」「图层」「3D」「2D/3D」；地图右上「显示模式：实时态势 ▾」。
- 中央区域：3D 地形地图；中部偏右为**红色方形锁定框（四角括号）**＋红色靶心标记，右上紧邻红色标签「目标003」；约 6 个蓝色无人机图标沿弯曲航迹排布，航迹为青色/浅蓝线（**带箭头**，有实线与虚线两种），其中一条浅蓝色实线由左侧无人机**直指目标**（引导线）；目标右上方一片绿色多边形区域；右下方一枚绿色环形地面节点图标，以绿色虚线连向目标区；全图另有一条大尺度青蓝色弧线横跨左侧。**图上无逐机文字标签**（无「攻击机」「评估机」字样）。地图左下浮层标签：「多源数据叠加 ●」「实时锁定 ●」「轨迹修正 ●」（每项后带绿色圆点）。按钮：「调整观察角度」（蓝色实心）「保持跟踪」（深色描边）。
- 右侧面板：①「AI提示」（内容置于小边框框内）：「目标区域出现移动迹象，建议调整光电集群观察角度。」②「跟踪状态」：「目标跟踪稳定度」→ 蓝色进度条约 87% ＋「87%」；「位置校正」→ 旋转加载图标 ＋「进行中」。③「协同链路」：「火力协同链路」→「稳定」；「雷达数据」→「持续校正」；「电子侦察」→「正常」（3 项数值均为绿色）。
- 底部：无按钮（按钮在地图左下浮层）；最下方为全局状态条 6 段（同通例逐字）。
- 配色与图形：深色底＋青蓝主色；目标锁定框与「目标003」标签为**红色**；航迹为青色实线/浅蓝虚线**带箭头**；绿色用于区域面与地面节点；有 3D 地形；**无视频窗口**（**未见**右下角毁伤视频窗口）。
- 图上出现的具体数值：「目标跟踪稳定度 87%」；目标编号「目标003」；顶栏 15:14。
- 与文字稿/Excel 描述**不一致**或**图上没有**的地方：
  - Excel：「右栏各机任务状态（攻击机：前往 ip 点/进入 ip 点/目标跟踪/俯冲攻击；评估机：前往评估位置/进入评估航线/目标跟踪/毁伤评估）」——**图上未见**任何逐机任务状态列表，右栏只有「跟踪状态／协同链路」两项汇总；也**无**「ip 点」字样，疑似描述有误。
  - Excel：「移动目标显示运动状态」——图上只有「AI提示：目标区域出现移动迹象」，**图上未见**航向/速度等运动状态字段。
  - Excel：「AI 提示遮挡→‘调整观察角度’按钮→重新规划评估航线」——按钮「调整观察角度」在图上**存在**；但图上 AI 提示原文为「目标区域出现移动迹象，建议调整光电集群观察角度。」（原因是"移动迹象"而非"遮挡"，疑似描述有误）；图上**未见**「重新规划评估航线」按钮或航线重规划提示。
  - Excel：「进入俯冲攻击后提示并放大、高亮攻击机轨迹」「命中后提示已命中、目标标签红变灰」「右下角开窗口播放毁伤视频」「AI 提示‘目标已摧毁，是否生成任务评估报告’→‘生成’」——以上**图上全部未见**（目标标签仍为红色，无命中提示、无视频窗口、无生成报告提示），疑似描述指向后续帧或描述有误。
  - 文字稿「集群轨迹动态运行」「目标区域实时锁定框」「多源数据叠加」「火力协同链路状态显示」「目标跟踪稳定度显示」——图上均有对应（虚线/实线航迹＋红色锁定框＋左下「多源数据叠加」标签＋右栏「火力协同链路：稳定」＋「目标跟踪稳定度 87%」）。
  - 图上**无**「命中概率」「偏差」等数值。

---

## T6-2.png —— 引导控制界面

- 整体版式：顶栏 ＋ 最左竖向图标导航 ＋ 中央大区（3D 地图占满，左下浮出 3 个状态标签与 2 个按钮）＋ 右侧 3 张面板（AI轨迹修正／命中概率／同步状态）；最底部全局状态条。
- 顶栏文字（逐字）：「智能任务管理系统」；「网络：已连接」「15:14」；左导航 7 项同通例。
- 左侧面板：无独立左侧面板（仅竖向导航条）。工具条：「选择」「测距」「图层」「3D」「2D/3D」；地图右上「显示模式：引导控制 ▾」。
- 中央区域：3D 地形地图；中部为红色方形锁定框＋红色靶心，右上**红色双行标签**「目标003」「位置更新」；由左下方无人机引出一条**青色实线弧线，末端蓝色箭头直指目标**（修正后引导轨迹）；另有浅蓝/灰色**虚线**（原轨迹）与**橙色虚线**（偏差指示，带箭头），橙色虚线旁标注「偏差 12m」（橙色）；由目标向右下绿色环形地面节点一条**绿色虚线＋箭头**，标注「信息同步」（绿色）；约 5 个蓝色无人机图标；地图右侧「N」「+」「−」。左下浮层标签：「轨迹已修正 ●」「目标锁定 ●」「信息同步 ●」（每项后绿色圆点）。按钮：「确认引导」（蓝色实心）「重新规划」（深色描边）。
- 右侧面板：①「AI轨迹修正」：「已根据目标移动趋势修正引导轨迹。」＋下方小示意图（青色实线曲线带箭头指向红色目标标记，其下为灰色虚线＝原轨迹）。②「命中概率」：环形进度「84%」（青/蓝圆环，起点一小段绿色），右侧「持续上升」＋右上趋势箭头＋横向条形进度（青→绿）。③「同步状态」：「目标位置」→「实时更新」；「火力引导信息」→「已同步」；「偏差修正」→「已生成」（3 项数值均为绿色）。
- 底部：无按钮（按钮在地图左下浮层）；最下方为全局状态条 6 段（同通例逐字）。
- 配色与图形：深色底＋青蓝主色；目标与标签为红色；偏差线为橙色虚线；信息同步线与地面节点为绿色；**图为"实线（修正后）＋虚线（原轨迹）并存、均带箭头"**；有 3D 地形；**无视频窗口**。
- 图上出现的具体数值：「命中概率 84%」；「偏差 12m」；目标编号「目标003」；顶栏 15:14。
- 与文字稿/Excel 描述**不一致**或**图上没有**的地方：
  - 文字稿「AI 实时修正轨迹／命中概率提示／偏差修正建议／目标位置实时更新／火力引导信息同步状态」——图上依次对应：「AI轨迹修正」（含示意图）、「命中概率 84% 持续上升」、地图「偏差 12m」标注＋右栏「偏差修正 已生成」、「目标位置 实时更新」、「火力引导信息 已同步」；但**「偏差修正建议」在图上没有文字化建议内容**（只有数值标注与"已生成"状态）。
  - 文字稿/Excel 均出现「ip 点」「评估航线」相关表述——本屏图上**均未见**。
  - 文字稿按钮【确认引导】【重新规划】——图上逐字一致（「确认引导」「重新规划」）。
  - 图上**无**目标运动速度/航向数值；**无**目标类型文字（仅有「目标003」编号）。

---

## T7-1.png —— 毁伤评估地图界面

- 整体版式：顶栏 ＋ 最左竖向图标导航 ＋ 中央大区（3D 地图占满，左下浮出 3 个状态标签与 2 个按钮）＋ 右侧 3 张面板（目标状态／集群回收／节点状态）；最底部全局状态条。
- 顶栏文字（逐字）：「智能任务管理系统」；「网络：已连接」「15:14」；左导航 7 项同通例。
- 左侧面板：无独立左侧面板（仅竖向导航条）。工具条：「选择」「测距」「图层」「3D」「2D/3D」；地图右上「显示模式：复核态势 ▾」。
- 中央区域：3D 地形底图（暗色、带密集网格/点云质感）；中偏左一枚**灰色**准星标记，其左上**灰底两行标签**「目标003」「已失效」；其右两处**黄色三角形警示图标**，各标注「残余威胁」（橙色字）；一条**浅蓝白虚线大环线**，环线上方标注「复核航线」；中下部一个灰色椭圆框标注「已清除」；多条**青色实线曲线带箭头**（返航轨迹）；右下方绿色环形地面节点，绿色箭头指向其右旁标注「节点待脱离」（绿色）；约 4～5 个蓝色无人机图标沿线排布；地图右侧「N」「+」「−」。左下浮层标签：「目标灰化 ●」「残余标记 ●」「返航轨迹 ●」（每项后绿色圆点）。按钮：「继续复核」（蓝色实心）「准备脱离」（深色描边）。
- 右侧面板：①「目标状态」：「目标003」→「已失效」（灰）；「区域状态」→「已复核」（绿）；「残余风险」→「低」（绿）。②「集群回收」：「回收进度」→「68 %」＋蓝→绿渐变进度条；「3个集群返航中」（灰色小字）。③「节点状态」：「前沿节点」→「待脱离」（绿）；「复核侦察」→「进行中」（蓝）；「链路释放」→「准备中」（蓝）。
- 底部：无按钮（按钮在地图左下浮层）；最下方为全局状态条 6 段（同通例逐字）。
- 配色与图形：深色底＋青蓝主色；**失效/已清除一律灰色**，残余威胁为**黄色三角警示**（橙字），返航航迹与节点为青/绿；航迹为青色实线**带箭头**，复核航线为浅色**虚线**；有 3D 地形；**无视频窗口**。
- 图上出现的具体数值：「回收进度 68 %」；「3个集群返航中」；目标编号「目标003」；顶栏 15:14。
- 与文字稿/Excel 描述**不一致**或**图上没有**的地方：
  - 文字稿「目标状态变化：红→黄→灰」——本图上目标**已是灰色**（「目标003 已失效」），**图上未见红色目标标记**；黄色只用于「残余威胁」标记，疑似描述把跨屏（多帧）状态压到一屏，具体三色渐变在本图**未见**。
  - 文字稿「区域残余威胁标记」——图上为黄色三角＋「残余威胁」×2，**已对应**；「集群回收轨迹」——图上为青色实线带箭头航迹，**已对应**（但无"回收"文字标签）；「复核侦察航线显示」——图上为虚线环＋文字「复核航线」，**已对应**；「前沿节点准备脱离提示」——图上右栏「前沿节点 待脱离」＋地图「节点待脱离」，**已对应**。
  - 图上**无**「红色」警戒态目标、**无**「毁伤评估」结果数值（无毁伤率/毁伤等级字段），**无**评估完成百分比。
  - 图上**无**时间轴、**无**任务编号、**无**「t0」「ip 点」。

---

## T7-2.png —— 任务总结界面

- 整体版式：顶栏 ＋ 最左竖向图标导航 ＋ 中央大区（上部约 3/5 为 3D 地图；下部依次为 3 张指标卡、「报告生成」进度行、3 个状态标签行、3 个按钮行）＋ 右侧 3 张面板（AI摘要／关键结果／后续处置）；最底部全局状态条。
- 顶栏文字（逐字）：「智能任务管理系统」；「网络：已连接」「15:14」；左导航 7 项同通例。
- 左侧面板：无独立左侧面板（仅竖向导航条）。工具条：「选择」「测距」「图层」「3D」「2D/3D」；地图右上「显示模式：结果汇总 ▾」。
- 中央区域：3D 地形底图（暗色网格质感）；中部偏上**灰色**准星标记＋灰底两行标签「目标003」「已清除」；一条**浅灰色虚线闭合航线**（复核/返航航线）；青色实线曲线带箭头汇入右下绿色环形地面节点，节点右侧绿底标签「可脱离」；2～3 个蓝色无人机图标沿线排布；地图右侧「N」「+」「−」。下部 3 张指标卡（左→右）：①准星图标＋「目标清除」＋大号「3」＋右上绿色对勾 ✓；②叠层图标＋「覆盖区域」＋大号「24.6 km²」＋右上绿色对勾 ✓；③六边形组网图标＋「协同效率」＋大号「91%」＋右上绿色对勾 ✓。「报告生成」一行：文档图标＋「报告生成」＋一条**绿色满进度条**（**左侧无百分比数字**）。状态标签 3 个：「信息包已推送 ✓」「残余风险低 ✓」「链路待释放 ●」（第 3 项为**蓝色圆点**，非对勾）。按钮 3 个：「返回场景选择」（深色描边）「导出任务报告」（蓝色实心，居中）「快速脱离体系」（深色描边）。
- 右侧面板：①「AI摘要」：「主要威胁节点已清除，建议保持区域持续监视。」＋右侧一枚大号**绿色对勾**（同心圆环）图形。②「关键结果」：「成功率」→「92%」（绿）；「组网时长」→「38分钟」（绿）；「推送结果」→「已送达」（绿）。③「后续处置」：「资源撤收」→「准备中」（蓝）；「链路释放」→「待确认」（橙）；「部署转移」→「可执行」（绿）。
- 底部：按钮行在地图下部（「返回场景选择」「导出任务报告」「快速脱离体系」）；最下方为全局状态条 6 段（同通例逐字）。
- 配色与图形：深色底＋青蓝主色；结果类数值与对勾为**绿色**，「待确认」为橙色，「准备中/进行中」为蓝色，「已清除/已失效」为灰色；实线（返航曲线）**带箭头**、虚线（航线）无箭头；有 3D 地形；**无视频窗口**，**无音频/语音播放控件**。
- 图上出现的具体数值：「目标清除 3」；「覆盖区域 24.6 km²」；「协同效率 91%」；「成功率 92%」；「组网时长 38分钟」；顶栏 15:14。
- 与文字稿/Excel 描述**不一致**或**图上没有**的地方：
  - 文字稿「AI 自动总结语音（‘任务完成，目标打击成功率92%，主要威胁节点已清除…’）」——图上**没有语音播放控件/波形/喇叭按钮**，只有文字块「AI摘要」，其原文为「主要威胁节点已清除，建议保持区域持续监视。」，**图上未见**「任务完成」「目标打击成功率92%」整句（92% 只出现在右栏「关键结果·成功率」），疑似描述有误。
  - Excel：「下方‘生成总结报告’、‘返回场景选择’按钮」——图上按钮为「返回场景选择」「导出任务报告」「快速脱离体系」，**无「生成总结报告」字样**（疑似 Excel/文字稿用词与图不一致；图上对应按钮是「导出任务报告」）。
  - Excel：「右栏任务摘要（打击目标类型/目标数量/任务持续时间/后续处置建议）」——图上右栏为「AI摘要／关键结果（成功率·组网时长·推送结果）／后续处置（资源撤收·链路释放·部署转移）」；**无「打击目标类型」**，「目标数量」以「目标清除 3」形式出现在**中部指标卡**而非右栏，疑似描述有误。
  - 文字稿报告 4 项「目标摧毁数量／覆盖区域／集群协同效率／局部组网持续时长」——图上对应为「目标清除 3」「覆盖区域 24.6 km²」「协同效率 91%」「组网时长 38分钟」（用词不同：摧毁→清除、集群协同效率→协同效率、局部组网持续时长→组网时长）。
  - Excel：「AI 提示‘目标已摧毁，是否生成任务评估报告’→‘生成’→任务总结界面」——本屏图上**未见**该提示与「生成」按钮（属上一屏缺失，见 T6-1/T6-2 条目）。
  - 图上**无**时间轴、**无**「t0」「ip 点」、**无**目标打击用时（除「组网时长 38分钟」外无其它时长）。
