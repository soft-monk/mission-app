# 参考文档（交接快照）

> **这是什么**：工作区 `webMap\参考文档\` 里那批**工作文档的快照**，为的是"换一台电脑、只 clone 仓库"
> 也能读懂这套工程 —— 那些文档原本不在任何仓库里，很容易在交接时丢掉。
>
> **纪律：快照不是源。** 要改就改工作区的 `参考文档\`，改完再重新快照一次（本目录整体覆盖）。
> 每次快照请同步更新下面这行"快照点"。

**快照点**：2026-09-17 · `mission-app` @ `e1af721`（P0–P7 完成 + P8 可用性收尾 + 手点通路打通）· 27 个 md / 约 720 KB

（**不含**参考图 `需求图与描述\场景1\*.png` —— 30+ MB，留在工作区，不入库。）

> **注意：宿主层没有单独的"需求专篇"**。12 篇 `*需求专篇.md` 都是**引擎/模块**的；
> 宿主（业务层 + 装配层）的需求与设计由第 2、3、4、8 项承载（实施方案 = 设计与裁决、流程接口冻结 = 施工契约、
> 引擎集成接口地图 = 引擎签名与接入待办、界面需求初稿 = 原始的界面/流程需求输入）。

---

## 按阅读顺序

| # | 文档 | 为什么读 |
|---|---|---|
| 1 | `交接文档.md` | **先读这份**：环境准备、已知坑、P0–P7 现状、怎么自己验一遍、并行验收的硬约束 |
| 2 | `业务层宿主层实施方案.md` | **宿主层的主文档**（`PLAN-APP-001`）：Excel 11 步映射、4 类本地配置、分阶段实施、**§10 裁决表（A1–A17）与 §10.1（Q1–Q10）** |
| 3 | `流程接口冻结.md` | **施工契约**：事件名、命令面、屏幕清单、文件归属、各阶段实测形状与命令序、已踩过的坑 |
| 4 | `引擎集成接口地图.md` | 16 个模块的准确签名（带 `文件:行号`）+ 宿主接入待办清单 —— 写宿主代码前查它 |
| 5 | `引擎缺陷登记.md` | 集成期发现的模块侧缺陷与"只声明未定义"的坑（已绕过，未改模块） |
| 6 | `模块划分与缺口分析.md` / `模块仓库清单.md` | 为什么这么切模块、20 个仓各自是什么 |
| 7 | `业务引擎需求专篇_汇总索引.md` + 12 篇 `*需求专篇.md` | 每个**引擎**的需求条目（实现与验收的唯一依据）；各模块仓内也各有一份 `docs/需求/` |
| 8 | `智能任务管理系统_界面需求初稿.md` | 界面与流程的原始需求（T0–T7 各屏元素、通用框架 §3） |
| 9 | `待裁决清单.md` | **尚未拍板**的产品口径（已裁决的按类分散在实施方案 §10/§10.1、protocol §4/§5、各变更请求单） |
| 10 | `*变更请求单.md`（`geo-data` / `map-2d` / `mapApp`） | 登记给各模块的改动请求（CR-01…） |
| 11 | `规则包改造M5M6.md` | M5/M6 规则包字段的现状、解析口径、补丁与风险（P5 已实施） |

## 三处"裁决日志"在哪

- **宿主/装配/流程类** → `业务层宿主层实施方案.md` §10（A1–A17）与 §10.1（Q1–Q10）
- **契约类**（事件名、规则包 schema、兄弟段登记） → `phase-engine/docs/契约/protocol.md` §4/§5
- **模块缺陷类** → 本目录的 `引擎缺陷登记.md` 与三份 `*变更请求单.md`
