# entity-ledger · 目标与情报台账引擎需求专篇

- 版本：v0.1（立项稿）
- 定位：把**"有哪些目标、谁最该打、它过去去过哪、编号在各界面是否一致"**抽为一个**独立可复用引擎**，供建仓、实现与验收直接使用
- 需求编号体系：`ELG-<域>-<序号>`；域见 §3
- 口径：**只有两种状态：✅ 已完成 ｜ ⏳ 待完成**（本专篇为立项文档，全部 ⏳）
- 分级用词：**MUST（必须，验收必查）/ SHOULD（应该，偏离需在决策记录说明）/ MAY（可选）**
- **部署形态：后端 C++ 仓**（与 `resource-alloc` / `scoring` 同构）
- 上游依据：
  - `docs/01-需求/智能任务管理系统_界面需求初稿.md`（§6 T4-1/T4-2、§8 T4-1/T4-2；§7.2 第三屏红方态势；**§10.12 目标台账口径待确认**）
  - `docs/02-技术需求/智能任务管理系统_技术需求文档.md`（§4.7 `TR-AI-03`；§4.13 `TR-TGT`；§4.8 `TR-BIZ-04`）
  - `docs/05-开发/开发接口契约规格书.md`（§3.5 目标接口、§4 `target.state` 事件、§7.5 演示数据）
  - `backend/src/core/Seed.cc`（目标播种数据）
  - [`模块仓库清单.md`](./模块仓库清单.md)、[`模块划分与缺口分析.md`](./模块划分与缺口分析.md) §3.6
- 配套文档（待建）：`entity-ledger 契约`、`entity-ledger 设计`

---

## 0. 完成情况总览

> 一句话：**共 41 条需求，全部 ⏳**（本专篇为立项，尚未开工）。
> 现有实现是**查询 + 四个动作开关**，没有评级引擎、没有编号一致性检查。

**现有资产映射**

| 现有实现 | 位置 | 已有 | 缺什么 |
|---|---|---|---|
| `targets()` | `backend/src/api/Api.cc` L712–715 | 按 `target_no` 排序返回 | 无筛选、无优先级排序、无一致性校验 |
| `targetDetail()` | `Api.cc` L717–732 | 演示级 AI 分析块：类型判断、行为、威胁等级、活动状态、处置建议、打击窗口、置信度 | **规则本身写在引擎里**：`type.find("指挥")` 子串匹配、`threat == "high"` 三元判断、**打击窗口是硬编码字符串**"预计 12 分 32 秒后出现暴露窗口"（L728） |
| `targetTrack()` | `Api.cc` L734–736 | 按 ts 升序返回轨迹点 | 无窗口裁剪、无抽稀、无预测 |
| `targetAction()` | `Api.cc` L738–759 | 四动作：`strike`/`upgrade`（含**幂等拒绝** `code=1002`）、`track`/`watch`（广播 `ai.event`）、落事件日志 | 语义词（"持续跟踪"/"重点监视"）**写在引擎里**；无状态机 |
| 目标表 | `backend/src/core/Seed.cc` | `target` / `target_track` 表与演示数据 | 属规则/宿主 |
| 前端索引 | `useStore.ts` `refreshTargets()` L361–368 | `targets` + `targetsByNo` 双索引 | 索引逻辑应在引擎侧 |
| 前端 | `useStore.ts` `selectTarget()` L394–405 | 选中 + 拉详情 + 拉轨迹 | 无逻辑 |

**关键差距（本专篇主要价值）**

> 1. **初稿 §10.12 记录的不一致至今未收口**：场景二 T4-1 是 5 个目标、T4-2 是 6 个、T7-2 是 7 个；
>    场景一 T4 高价值标红是 **002**、文字稿写 **003**。今天的代码**不做任何一致性检查**，所以这个矛盾会一直在。
> 2. **威胁评级不是算出来的**：`targetDetail` 直接读库里 `threat` 字段，再用 `threat == "high"` 决定建议——
>    没有打分、没有权重、没有可复算过程。`TR-AI-03` 要求"按目标类型 + 距离打分"，今天没有实现。
> 3. **打击窗口是一个写死的字符串**（`Api.cc` L728），不是根据轨迹与暴露规律算出来的。

---

## 1. 定位与边界

### 1.1 定位

```
输入：候选目标观测（来自 device-ingest 归一化事件 / 人工录入 / 融合结果）
   │
   ▼
┌──────────────────────────────────────────────────────────┐
│  entity-ledger（C++，无 Web 框架、无 SQL）                 │
│  REG 实体登记 → ID 编号与一致性 → RATE 威胁评级 →           │
│  RANK 优先级排序 → TRK 轨迹服务 → ACT 动作状态机            │
│                      │                                   │
│                      └──► 出口：IEntitySink（宿主实现）      │
└──────────────────────┬───────────────────────────────────┘
                       │ 台账快照 + 变更事件（target.state）
                       ▼
   宿主：落库 + 广播 ｜ scoring：读优先级 ｜ view-composer：读图元
```

**一句话**：entity-ledger 回答"**这个实体是什么、有多危险、排第几、去过哪**"。
它不回答"打不打它"（`scoring` 出方案）、也不回答"它在地图上画成什么符号"（`view-composer` + `map-2d`）。

### 1.2 为什么必须是独立引擎

1. **它是唯一一处能收口编号一致性的地方**：只要台账是唯一权威、且引擎提供一致性检查，初稿 §10.12 的矛盾才有解。
2. **它有两个消费者**：换一套指挥系统，实体类型与评级口径会变，但**台账 + 编号 + 轨迹 + 动作状态机相同**。
3. **它被 `scoring` 依赖**：优先级是打击方案的输入。

### 1.3 范围边界

| 做 | 不做 |
|---|---|
| 实体登记：类型、坐标、来源、置信度、动态状态 | 🚫 不含具体**实体类型名**（机动指挥节点/防空火力阵地/通信保障节点 属规则） |
| **编号规则与一致性检查**（任务内唯一 `target_no`） | 🚫 不做打击方案与成功率（`scoring`） |
| 威胁评级引擎（打分，可复算、可解释） | 🚫 不做地理符号与配色（`view-composer` / `map-2d`） |
| 优先级排序与序列（加入攻击序列） | 🚫 不做告警产生（`alert-engine`，本引擎只发变更） |
| 轨迹服务：写入、区间查询、**裁剪与抽稀**、时间轴输出 | 🚫 不做时间轴播放控件（`map-2d` 已有 `ReplayBar`） |
| 动作状态机：持续跟踪 / 重点监视 / 升级打击 / 打击，含幂等与互斥 | 🚫 不做毁伤判定（属评估口径，见 §9 待确认） |
| 情报关联关系（节点链） | 🚫 不做自然语言分析文案（`llm-provider`） |

### 1.4 硬约束

| 约束 | 取值 | 依据 |
|---|---|---|
| 现有接口形状 | `/targets`、`/targets/{id}`、`/targets/{id}/track`、`/targets/{id}/{action}` 返回形状**不得破坏** | 契约 §3.5；`Api.cc` L712–759 |
| `target.state` 事件 | 负载含 `{targetId, targetNo, threat, confidence, dynamicState, lng, lat, status:"red"\|"yellow"\|"gray"}` | 契约 §4 |
| 动作幂等 | `upgrade` 已执行时 MUST 返回 `code=1002 正在执行/已执行`（现状行为，保留） | `Api.cc` L744 |
| 动作参数 | `action ∈ {track, strike, upgrade, watch}` | `Api.cc` L738；前端 `useStore.ts` L449 |
| 事件日志 | `target.strike` / `target.upgrade` MUST 落事件日志 | `Api.cc` L748 |
| 编号口径 | **任务内唯一 `target_no`（整数）**，跨界面强一致 | 契约 §2；初稿 §10.12 |
| 演示数据 | 场景二 T4-1 五个目标；来源含融合/雷达/光电/电子；动态状态含机动中/活跃/部署中/转移中/短暂停留 | 初稿 §8 T4-1 |
| 语言/构建 | C++17 + CMake ≥ 3.20；仅 `nlohmann/json` | 对齐既有 C++ 模块 |

---

## 2. 已确认的输入约束

| 项 | 结论 | 来源 |
|---|---|---|
| 台账唯一性 | **单一目标台账**：所有界面从同一份渲染，不各自维护副本 | `useStore.ts` L2 注释；`TR-TGT-01` |
| 编号维度 | 以**任务**为维度唯一编号 | 契约 §2 |
| 威胁等级 | 三档 `high`/`mid`/`low`（配色红/黄/青） | 初稿 §6 T4-1；`map-2d` 调色板 |
| 目标状态色 | `red`/`yellow`/`gray`（毁伤状态 红→黄→灰） | 初稿 §6 T7-1；契约 §4 |
| 评级输入 | 目标类型 + 距离（`TR-AI-03` 原文） | TRD §4.7 |
| 四类来源 | 光电 / 雷达 / 电子 / 融合 | 初稿 §8 T4-1 图例 |
| 轨迹回放窗口 | 场景二 T4-2 为 `-02:00 ~ +02:00` | 初稿 §8 T4-2 |

---

## 3. 功能需求条目

### 3.1 实体登记（ELG-REG）

| 编号 | 需求描述 | 验收标准 | 状态 |
|---|---|---|---|
| ELG-REG-01 | **MUST** 实体类型由规则声明（`{ key, 名称, 基础威胁, 属性 }`），引擎 MUST NOT 内建类型名 | 换规则即换类型集；全仓检索 `机动指挥节点` 零命中 | ⏳ |
| ELG-REG-02 | **MUST** 台账字段：`{ id, targetNo, 类型, 坐标, 来源, 置信度, 动态状态, 威胁等级, 优先级, 关系链, 时间 }` | 字段完整可序列化；缺省字段有定义行为 | ⏳ |
| ELG-REG-03 | **MUST** 按任务隔离，且同一任务内 `targetNo` 唯一 | 重复 `targetNo` 登记被拒绝并给可读原因 | ⏳ |
| ELG-REG-04 | **MUST** 多源汇入去重：同一实体的多来源观测 MUST 能合并（按规则声明的主键或空间/类型相似度） | 同一目标三条来源观测 → 合并为一条且来源标注为多源 | ⏳ |
| ELG-REG-05 | **MUST** 置信度可更新：新观测到达时按规则融合（不是简单覆盖） | 融合规则可配；结果可复现 | ⏳ |
| ELG-REG-06 | **SHOULD** 动态状态机：状态迁移（如 短暂停留 → 机动中）由规则声明合法迁移 | 非法迁移被拒绝并给原因 | ⏳ |

### 3.2 编号与一致性（ELG-ID）

> 本域是**初稿 §10.12 的唯一收口点**。

| 编号 | 需求描述 | 验收标准 | 状态 |
|---|---|---|---|
| ELG-ID-01 | **MUST** 编号规则可配：序号生成（连续 / 不复用 / 起始值）由规则声明 | 删除中间目标后新增，编号行为符合规则 | ⏳ |
| ELG-ID-02 | **MUST** 一致性检查接口：给定一组"视图"或"界面集合"，校验 `targetNo` 与类型在各视图下是否一致，输出**逐条差异报告** | 人为制造"T4-1 五个 / T7-2 七个"的场景 → 报告定位到具体编号与视图 | ⏳ |
| ELG-ID-03 | **MUST** 全集与子集语义显式：提供"某阶段应可见的目标集合"查询（避免各界面自行过滤导致数量不一致） | 同一阶段两次查询结果一致；跨阶段集合变化可解释 | ⏳ |
| ELG-ID-04 | **MUST** 类型与编号绑定校验：同一 `targetNo` 的类型 MUST NOT 在不同阶段变化（除显式的"类型重判"动作） | 制造类型漂移 → 被检出；走重判动作 → 允许并留痕 | ⏳ |
| ELG-ID-05 | **SHOULD** 高价值标记唯一性：同一任务内"高价值"标记的数量与去向可查（初稿里 002/003 之争） | 可查询高价值集合；越界时报错 | ⏳ |

### 3.3 威胁评级（ELG-RATE）

| 编号 | 需求描述 | 验收标准 | 状态 |
|---|---|---|---|
| ELG-RATE-01 | **MUST** 评级规则可注入：因子清单（类型基础威胁、距离、活动状态、防护强度、电子干扰等）、权重、分档阈值全部由规则声明 | 换规则即换评级；引擎内无 `"high"` 字面量判断 | ⏳ |
| ELG-RATE-02 | **MUST** 打分可复算：输出逐因子得分与总分，导出后可手算核对 | 任取一目标，导出项手算等于总分 | ⏳ |
| ELG-RATE-03 | **MUST** 分档由阈值决定（不是布尔判断）：`high`/`mid`/`low` 的边界可配 | 调阈值后同目标分档变化可复现 | ⏳ |
| ELG-RATE-04 | **MUST** 距离因子用真实几何：以任务参考点（或指定基准）计算距离，不得用占位值 | 给定两点坐标，距离手算一致 | ⏳ |
| ELG-RATE-05 | **MUST** 评级输入显式：为结构化快照，引擎 MUST NOT 读全局状态或数据库 | 单测只传快照即可评级 | ⏳ |
| ELG-RATE-06 | **MUST** 打击窗口由计算得出：现状硬编码字符串（`Api.cc` L728）MUST 移除；窗口 MUST 由轨迹与规则（暴露规律）推算，并输出为**结构化时间区间** | 全仓检索 `12 分 32 秒` 零命中；窗口为可计算的时间区间 | ⏳ |

### 3.4 优先级与序列（ELG-RANK）

| 编号 | 需求描述 | 验收标准 | 状态 |
|---|---|---|---|
| ELG-RANK-01 | **MUST** 排序规则可配：默认按威胁等级 + 优先级字段，次级键稳定 | 同分排序稳定可复现 | ⏳ |
| ELG-RANK-02 | **MUST** 优先级值可显式设置（初稿 T5-2：优先级 1/2/3） | 设置后排序与界面一致 | ⏳ |
| ELG-RANK-03 | **MUST** 打击序列：支持加入/移出序列，序列内唯一、可排序（"加入攻击序列"） | 重复加入幂等；移出后序号键稳定 | ⏳ |
| ELG-RANK-04 | **SHOULD** 序列变更可审计：谁在什么时候把谁加入了序列 | 可从事件日志复原序列变化 | ⏳ |

### 3.5 轨迹服务（ELG-TRK）

| 编号 | 需求描述 | 验收标准 | 状态 |
|---|---|---|---|
| ELG-TRK-01 | **MUST** 轨迹写入：按 `{targetId, ts, lng, lat, ...}` 追加，MUST 按 ts 有序 | 乱序写入后查询仍有序 | ⏳ |
| ELG-TRK-02 | **MUST** 区间查询：按 `[from, to)` 取点，支持分页/上限截断且**不静默丢** | 超限时返回截断标记与游标 | ⏳ |
| ELG-TRK-03 | **MUST** 抽稀：按 `everyNth` 或最小时间间隔抽稀，且**如实标注已抽稀** | 抽稀后点数下降且标注存在 | ⏳ |
| ELG-TRK-04 | **MUST** 时间轴输出：产出可直接喂给回放组件的结构（对齐 `map-2d` M2-DRAW-18 的入参形状） | 输出可被 `map-2d` 回放接口直接消费（由宿主适配） | ⏳ |
| ELG-TRK-05 | **SHOULD** 预测轨迹：按规则外推若干点并**显式标注为预测**（初稿 T6-1 "预测轨迹"虚线） | 预测点带标记，不与实测点混淆 | ⏳ |
| ELG-TRK-06 | **SHOULD** 轨迹保留：过期点清理可配（避免无界增长） | 配置后旧点被清理且计数可查 | ⏳ |

### 3.6 动作状态机（ELG-ACT）

| 编号 | 需求描述 | 验收标准 | 状态 |
|---|---|---|---|
| ELG-ACT-01 | **MUST** 动作集由规则声明（现状四动作，MUST 支持扩展），引擎 MUST NOT 内建动作名 | 换规则即换动作集；全仓检索 `watch` 零命中 | ⏳ |
| ELG-ACT-02 | **MUST** 幂等：已升级/已打击的重复请求 MUST 视为**幂等成功**，按冻结契约返回 `code=0` + `data.idempotent=true`，并在结果中用 `status="already-done"` 承载"已执行"语义；**MUST NOT** 为非零码（`code=1002` 仅用于互斥冲突，对齐 `protocol.md` §3.3 CTR-EC-01/02/03 与 ADR-C15） | 连点两次 upgrade：第二次 `code=0`、`idempotent=true`、`status="already-done"`，前端不抛错；宿主 gate 内重入另测返回 `1002` | ⏳ |
| ELG-ACT-03 | **MUST** 动作产出结构化事件（`target.state`），**NOT 自然语言文案**：现状把"已开始持续跟踪 X"写在引擎里（`Api.cc` L750–756）MUST 移除，文案归 `llm-provider` / 规则包 | 全仓检索 `已开始持续跟踪` 零命中；动作只产出状态变更 | ⏳ |
| ELG-ACT-04 | **MUST** 动作前置条件可声明（如"必须先升级为打击目标才能打击"） | 未满足前置时拒绝并给可读原因 | ⏳ |
| ELG-ACT-05 | **MUST** 动作落事件日志（对齐现状 `target.strike`/`target.upgrade`） | 日志可查、可复原 | ⏳ |
| ELG-ACT-06 | **SHOULD** 动作可撤销语义显式：可撤销/不可撤销由规则声明 | 尝试撤销不可撤销动作被拒绝 | ⏳ |

### 3.7 情报关联（ELG-INTEL）

| 编号 | 需求描述 | 验收标准 | 状态 |
|---|---|---|---|
| ELG-INTEL-01 | **MUST** 关系链：实体间可声明有向关系（初稿 §7.2 "通信节点 → 指挥节点 → 火力阵地 → …"） | 关系可增删查；成环被检出 | ⏳ |
| ELG-INTEL-02 | **MUST** 关联查询：可查某实体的上下游链路、可查"关联目标数"（第三屏统计项） | 链路查询结果可复现 | ⏳ |
| ELG-INTEL-03 | **SHOULD** 已确认/疑似两态：支持 `confirmed` / `suspected` 标记（第三屏虚线框 + 问号） | 两态可筛选、可统计 | ⏳ |

### 3.8 非功能性（ELG-NFR）

| 编号 | 需求描述 | 验收标准 | 状态 |
|---|---|---|---|
| ELG-NFR-01 | **MUST** 零外部依赖：无 Web 框架、无 SQL（持久化走注入的 `IEntityStore`）；不依赖其它引擎内部 | 空环境独立构建；跨仓 import 零命中 | ⏳ |
| ELG-NFR-02 | **MUST** 出口为反向接口：`IEntitySink` 由宿主实现用于落库与广播 | 引擎内无落库与广播 | ⏳ |
| ELG-NFR-03 | **MUST** 确定性：同输入同输出；评级与排序 MUST 可复现 | 同序列跑两次结果一致 | ⏳ |
| ELG-NFR-04 | **MUST** 独立交付：独立构建、独立示例、独立测试、独立验收脚本（退出码 0/1） | 空环境 clone → 一条命令构建 → 一条命令验收 | ⏳ |
| ELG-NFR-05 | **SHOULD** 性能：1000 实体台账下评级全量 ≤ 10 ms；轨迹区间查询（1 万点）≤ 5 ms | 实测达标 | ⏳ |

---

## 4. 引擎与规则的划分（policies）

| 属引擎（可复用） | 属规则（`policies/mapapp`，项目专属） |
|---|---|
| 登记、去重、置信度融合机制 | **实体类型清单**：机动指挥节点 / 防空火力阵地 / 通信保障节点 … |
| 编号生成与**一致性检查** | **编号起始值与是否复用** |
| 评级引擎（因子求值、加权、分档） | **评级因子与权重、分档阈值** |
| 排序与打击序列机制 | **默认排序偏好**、序列上限 |
| 轨迹写入/查询/抽稀/时间轴/预测框架 | **抽稀参数、预测外推规则** |
| 动作状态机与幂等 | **动作清单与前置条件**（track/watch/strike/upgrade 的语义） |
| 关系链与已确认/疑似两态 | **动态状态取值集**（机动中/活跃/部署中/转移中/短暂停留） |

**判据**：引擎公开接口里出现 `机动指挥节点`、`高价值`、`预计 12 分 32 秒`、`已开始持续跟踪` 即破线。

---

## 5. 决策记录

| 编号 | 议题 | 决策 | 影响 |
|---|---|---|---|
| D1 | 引擎部署侧 | **后端 C++** | 全部条目 |
| D2 | 编号一致性的收口方式 | **引擎提供检查接口 + 阶段可见集合查询**，由宿主在关键界面渲染前调用；不指望"约定" | ELG-ID-02/03；初稿 §10.12 |
| D3 | 打击窗口 | **从硬编码字符串改为计算输出**（结构化时间区间） | ELG-RATE-06；R2 风险 |
| D4 | 动作产生的文案 | **移除**；文案归 `llm-provider` / 规则包 | ELG-ACT-03 |
| D5 | AI 分析块（`targetDetail` 的 `ai` 字段） | 现状是"引擎内写死规则"；**改为规则驱动 + 可复算**，形状尽量保持以兼容前端 | ELG-RATE-01/02 |
| D6 | 与 `scoring` 的边界 | 本引擎出**优先级与序列**；`scoring` 出**方案与成功率**。策划分工不重叠 | ELG-RANK-* |
| D7 | 毁伤状态（红→黄→灰）归谁 | **归本引擎的状态字段**（`status`），但"何时变灰"的口径属评估，见 §9 待确认 2 | 契约 §4 |

---

## 6. 风险

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| R1 | 一致性检查会**暴露现有演示数据的矛盾**（五个/六个/七个） | 演示"自证不一致" | 这正是 D2 的目的：先用检查把差异列出来，再由 `mission-baseline` 定一份权威集合；演示数据同步修正 |
| R2 | 打击窗口改为计算后，数值与截图不一致 | 界面回归 | 按演示口径配置规则，使计算结果复刻截图值；同时保留"规则可调" |
| R3 | 多源去重规则不当，把两个真目标合成一个 | 漏目标 | 去重规则必须可配且**默认保守**（宁可标多源不合并），并提供人工拆分 |
| R4 | 评级抽象过度（做成通用评分框架） | 与 `scoring` 重复、配置地狱 | 只支持"因子线性加权 + 阈值分档"，不做多目标优化 |
| R5 | 轨迹无界增长 | 存储与查询退化 | ELG-TRK-06 保留策略；实测门槛 |
| R6 | 关系链成环导致查询死循环 | 服务挂起 | ELG-INTEL-01 成环检出设为 MUST |

---

## 7. 验收清单

- [ ] 空环境 clone → 一条命令构建 → 一条命令跑通验收脚本（退出码 0）
- [ ] 全仓检索实体类型名、`高价值`、`12 分 32 秒`、`已开始持续跟踪` → 零命中
- [ ] 重复 `targetNo` 登记被拒绝
- [ ] 同一目标三条来源观测合并为一条，来源标为多源；默认保守不误合
- [ ] 制造"某视图多两个目标" → 一致性报告定位到具体编号与视图
- [ ] 同一阶段两次查询可见集合一致
- [ ] 制造类型漂移 → 被检出；走重判动作 → 允许并留痕
- [ ] 任取一目标导出逐因子得分，手算等于总分；调阈值后分档变化可复现
- [ ] 打击窗口为结构化时间区间，可复算
- [ ] 连点两次 upgrade，第二次得到 `code=1002`
- [ ] 动作只产结构化事件，不产自然语言
- [ ] 未满足前置条件的动作被拒绝并给原因
- [ ] 关系链成环被检出
- [ ] 轨迹乱序写入后查询有序；超限返回截断标记；抽稀带标注
- [ ] 轨迹时间轴输出可被 `map-2d` 回放接口消费（由宿主适配）
- [ ] 预测点带显式标记，不与实测点混淆
- [ ] 1000 实体评级全量 ≤ 10 ms；1 万点区间查询 ≤ 5 ms

---

## 8. 需求追踪

| 域 | 条目 | 编号区间 |
|---|---|---|
| ELG-REG 实体登记 | 6 | 01–06 |
| ELG-ID 编号与一致性 | 5 | 01–05 |
| ELG-RATE 威胁评级 | 6 | 01–06 |
| ELG-RANK 优先级与序列 | 4 | 01–04 |
| ELG-TRK 轨迹服务 | 6 | 01–06 |
| ELG-ACT 动作状态机 | 6 | 01–06 |
| ELG-INTEL 情报关联 | 3 | 01–03 |
| ELG-NFR 非功能性 | 5 | 01–05 |
| **合计** | **41** | — |

**与既有需求编号的对应**

| 既有编号 | 本专篇覆盖 |
|---|---|
| `TR-AI-03` 威胁评级与优先级（按类型 + 距离打分） | ELG-RATE-*、ELG-RANK-* |
| `TR-BIZ-04` 目标管理 | ELG-REG-*、ELG-ACT-*、ELG-TRK-* |
| `TR-TGT-01` 单一目标台账 | ELG-REG-03、ELG-ID-02/03 |
| 契约 §3.5 `/targets*` | ELG-NFR-02（宿主落库）、接口形状冻结 |
| 契约 §4 `target.state` | ELG-ACT-03 |
| 初稿 §10.12 目标台账口径 | ELG-ID-*（**本引擎是其唯一收口点**） |
| 初稿 §7.2 第三屏红方态势 | ELG-INTEL-01/02/03 |
| `map-2d` M2-DRAW-17/18 回放 | ELG-TRK-04（输出对齐，模块零改动） |

---

## 9. 待确认（阻塞项）

| # | 待确认 | 阻塞条目 |
|---|---|---|
| 1 | **目标台账权威口径**：是否"以任务为维度唯一编号、跨界面强一致"（初稿 §10.12） | ELG-ID-02/03（本引擎全部一致性能力的前提） |
| 2 | 毁伤状态（红→黄→灰）的**判定口径与责任方**（引擎 or 评估模块） | D7、ELG-ACT-* |
| 3 | 威胁评级因子与权重（初稿 §10.2） | ELG-RATE-01/02/03 |
| 4 | 打击窗口的计算依据（轨迹外推？暴露规律表？） | ELG-RATE-06 |
| 5 | 多源去重的权威规则（同类型近距离？融合结果优先？） | ELG-REG-04 |
| 6 | 轨迹是否需要"存储到 `telemetry-store`"而非自留（避免两份历史） | ELG-TRK-06 |
