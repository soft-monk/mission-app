# 需求确认单（只记已确认的需求）

> **这份文档是什么**：只记录**你（需求方）明确确认过的**需求，一条一条追加。
> **不写**我没经你确认的东西 —— 不推测、不补全、不"顺手"加。
>
> **怎么用（你定的规矩，我照办）**
> 1. **一个界面一个界面确认**：当前这屏你确认完，才进下一屏。
> 2. **除了你点名的，其余一切不改**。我看着不对的地方，只**提出来问你**，不擅自改。
> 3. **缺图标你自己找**，我不去找。
> 4. **你没说"加"的，我不往这份文档里写**；一屏**你说"完成"之后**，才允许往下加下一屏的条目。
>
> 创建：2026-09-18 ｜ 最后更新：2026-09-18

## 状态口径

| 状态 | 含义 |
|---|---|
| 待确认 | 需求已记下、也按它实现了（有真机截图），但**你还没说"这屏可以了"** |
| **已确认完成** | **你说过这屏可以了** |
| 未开始 | 记下了，但还没做（目前没有这种条目） |

---

# SH-01 · 启动加载界面 ✅ 已确认完成

参考图：`参考文档\需求图与描述\场景1\系统启动界面一.png`

**屏状态：已确认完成**（2026-09-18 你说"启动界面完成了"）

| # | 需求（按你的原话） | 状态 |
|---|---|---|
| 1 | 「多出来的（瓦片目录已就绪 / 链路探针正常 / AI 未配置（规则未就绪）（红字）/ 资源台账就绪 / SQLite 可读写）**不要添加**」→ 模块进度卡里不显示第 4 行小字，只保留 图标 + 名称 + 细进度条 + 百分比 | 已确认完成 |
| 2 | 「当前来个**调试按钮**，**不点击，就无法进入下一界面**，后续该按钮会被删除的」→ 调试期加载完成后先不自动跳转，屏上给一枚调试按钮，点了才进下一屏 | 已确认完成（该按钮已按 #11 撤下） |
| 3 | 「**使用我提供的图片做底**」→ 启动页底图用你提供的图（`earth.png`） | 已确认完成 |
| 4 | 「**副标题固定「系统启动中，请稍候…」**，加载完成是直接跳转下一界面，但是 2. 我需要调试，所以**先不自动跳转**，但是**这个功能需要有**」→ 副标题为固定文案；"加载完自动跳下一屏"的能力**保留在代码里** | 已确认完成 |
| 5 | 「我去找了素材库，**添加一下** `C:\Users\softmonk\Desktop\png库`」→ 素材库的图标接进界面：右侧「系统状态」四行用 网络连接 / 数据链接 / 定位 / 安全 四个图标；顶栏 wifi 与用户头像见 #6 #9 #10 | 已确认完成 |
| 6 | 「启动界面**可以 ping 百度**，**能 ping 就使用 wifi 有，不能就用 wifi 无**」→ 顶栏 WiFi 按真实外网连通性切换图标 | 已确认完成 |
| 7 | 「启动界面下方的**「系统启动中，正在初始化核心模块」怎么没有了**」→ 底部提示条恢复成参考图那一行固定文案 | 已确认完成 |
| 8 | 「**这个栏太小了，高度变高点**」→ 底部提示条加高（34 → 48） | 已确认完成 |
| 9 | 「**右上角 wifi 图片我换了，你更新一下**」→ 换用你新给的 wifi 图标（`WIFI.png` / `无wifi.png`） | 已确认完成 |
| 10 | 「**用户图标加上**」→ 顶栏最右的头像用你提供的用户图标（`用户.png`） | 已确认完成 |
| 11 | 「启动界面完成了……**删除调试的进入下一屏幕按钮吧，我需要跑一次自动进入自检界面的流程**」→ 撤下那枚调试按钮，恢复"启动加载完成 → 自动进自检 SH-02"的流程 | 已确认完成 |
| 12 | 「**启动界面太快了，加载总长度 30 秒左右**，修改一下」→ 启动节拍 `boot.run{pacingMs}` 由 400 改 **6000**（5 个模块 × 6 秒）；实测**从打开页面到进入自检 30.7 秒**。`demo.ps1 runAll` 走它自己的节拍，**不受影响** | 已确认完成 |
| 13 | 「**启动动画为 5-15 秒范围内，当前太慢了**」→ 启动节拍 `boot.run{pacingMs}` 由 6000 改 **1500**（5 个模块 × 1.5 秒 ≈ **8.2 秒**，按第 12 条的实测口径折算）。只改前端这一处，宿主未动；`demo.ps1 runAll` 仍走它自己的节拍，**不受影响** | 2026-09-21 已改，**复测由用户手动做**（本轮未跑探针、未起服务、未重建产物） |

**附：落点（仅供追溯，不是需求）**
`apps/web/src/screens/BootScreen.tsx`、`apps/web/src/screens/Chrome.tsx`、`apps/web/src/App.tsx`；
素材 `apps/web/public/`（`earth.jpg` / `wifi-on.png` / `wifi-off.png` / `user.png` / `status-*.png`）。
调试闸门开关 `BOOT_HOLD_FOR_DEBUG`（现为 `false` = 交付形态）保留在 `BootScreen.tsx`，
需要再调试启动页时改回 `true`，那枚按钮会自己回来。

**附：第 12 条带出来的一个真缺陷（已修，如实登记）**
把启动撑到 30 秒之后暴露：**启动跑到一半时来一发 `boot.reset`，会把应用永久钉死在启动页** ——
reset 清掉引擎的进度项，而在飞的那一轮启动还在继续写，第一个模块（map）的 100 被清掉后再没补回来，
总进度只能到 80，`bootComplete()` 永不成立（实测：步 1 / overall=80 / complete=false，一路不复原）。
启动只要 2 秒时这个窗口极小，30 秒就变成常踩。**修法**：加"启动代号" `bootGen_` ——
`startBoot` 领号带进工作线程，`bootRunOnce` 每进一个模块比对一次，号对不上立即收手、**一个字都不写**；
`boot.reset` 只把号 +1。修复后实测：干净跑 30 秒到 100%/步 2；被打断则干净复位到 0%（点【重新加载】即可重来）。
落点：`packages/host/include/ma/flow.h`（`bootGen_`）、`packages/host/src/flow.cc`（`bootRunOnce` / `startBoot` / `boot.reset`）。

---

# SH-02 · 自检校验界面 ✅ 已确认完成

参考图：`参考文档\需求图与描述\场景1\系统启动界面二.png`

**屏状态：已确认完成**（2026-09-18 你确认并让我写进本单）

| # | 需求（按你的原话） | 状态 |
|---|---|---|
| 1 | 「左侧系统状态自检太小了，**将宽度扩大**，起码能一行显示『提示：自检过程预计耗时 15~30 秒』**还有盈余最佳**」→ 左栏 250 → **320**。实测：那行文字实宽 181px，改后内容宽 288px，**盈余 107px**，常见字体设置下都稳单行 | 已确认完成 |
| 2 | 「**宿主"没有卫星链路"，修改配置**」→ 卫星链路不再由宿主写死，改由**配置**决定：`config.json` 的 `selfcheck.satcomLinkUp`（本场景声明为 `true`）。同组的数传/组网不受它影响，仍取实测 | 已确认完成 |
| 3 | 「**重新检测，速度太快**，也是需要**按照 15~30 秒流程重来一次**」→ 自检（一键自检与重新检测都走这条路）改成**逐项重检 + 项间 4 秒节拍**；实测**总耗时 18.2 秒**，按钮显示 `自检中… N/5` | 已确认完成 |
| 4 | 「**xxx 检测，素材已经放到文件夹下**」→ 五张自检卡用素材库图标（通信链路检测 / 定位系统检测 / 集群节点检测 / 后方指控检测 / 系统安全检测），按**宿主给的 key** 认领（`comm`/`position`/`cluster`/`command`/`security`） | 已确认完成 |
| 5 | 「左下角，每个界面都有的第几阶段那个去除……删除吧」（你列在本屏下，但它**作用于所有屏**）→ 记在下面的**「全局」**一节 | 已确认完成 |

**附：落点（仅供追溯，不是需求）**
`apps/web/src/screens/SelfCheckScreen.tsx`（左栏宽度 / 逐项节拍 / 卡片图标）、`apps/web/src/App.tsx`（逐项重检的回调）；
宿主 `packages/host/src/main.cc`（卫星链路读配置）、`src/config.cc` + `include/ma/config.h`（新增 `selfcheck.satcomLinkUp`）、
`src/flow.cc`（自检前先刷新能力快照 —— 见下面的"一处未点名但必须做的"）；
素材 `apps/web/public/check-*.png`。

**一处未点名但必须做的（如实记在这里）**：实测发现同一块里的**组网链路**会红（`mesh-down`），
原因是探针在注册时**按值捕获**能力快照，而开机自检跑在浏览器 WS 连上之前；之后客户端连上也不会让**报告**自己重算
（"重新检测"用的仍是旧快照 → 永远红）。故在 `selfcheck.run` / `selfcheck.recheck` 开头各加了一次
`refreshCapabilities()`。实测：点一次【重新自检】→ 通信链路检测转 **normal**（三条子项全绿）。
**残留**：刚进本屏那一眼，组网链路可能仍显示红（开机自检跑在浏览器连上之前）；点一次重新自检即全绿。

---

# 全局（作用于所有屏）

| # | 需求（按你的原话） | 状态 |
|---|---|---|
| G-1 | 「**左下角，每个界面都有的第几阶段那个去除，没必要使用，演示也不需要，删除吧**」→ 左下角流程徽标 `步 N/11 · 屏名 · 阶段 Tx` 已从**所有屏**删除。"当前在哪一屏"改由已有的只读探针 `window.__flowStats().screen` 提供（四个验收脚本口径同步改过来，未留隐藏假元素） | 已确认完成 |

**附：落点与影响（仅供追溯，不是需求）**
`apps/web/src/App.tsx`（删组件 + 四处引用 + 样式；`Probe` 增加 `screen` 字段）；
`scripts/` 下 `click-check.mjs` / `click-real.mjs` / `screenshots.mjs` / `p3-check.mjs` 改读探针。
**一处如实说明**：该徽标原先还兼着"命令失败 code=…"的常驻提示；删除后这条信息只在各屏自己的红字回执
与「命令回执」折叠块里出现，**不再有全局常驻提示**。

---

# SH-03 · 任务态势主界面 ⏳ 待确认

参考图：`参考文档\需求图与描述\场景1\T0-1*.png`（态势屏）

**屏状态：待确认**（需求已记下、也按它实现了、每条都有实测自证，但**你还没说"这屏可以了"** ——
按本单第 1 条规矩，状态就如实停在"待确认"）

| # | 需求（按你的原话） | 状态 |
|---|---|---|
| 1 | 「默认显示的鼠标坐标，**显示的东西被遮挡了**？如果是，那么**修改 map2d 接口，要求不只是 true false，可以自己使用配置文件和哪些之前写的配置文件一起，可配置显影与位置**」→ **改 map-2d 接口**：新增 `ControlSpec{key,on,anchor,offset}` 与 `mapCommands.configureControls([...])`；`CoordReadout` / `Compass` 支持**锚点（四角）+ 像素偏移**；宿主从**已有配置文件** `data/scenario-1/map-style.json` 新增的 `controls` 段读（与 drone/track/groupColors 放一起）。**实测**：坐标读数此前**根本没显示**（`controls.coords` 默认 false + 组件要宿主渲染而宿主没渲染）—— 不是被遮挡；现在已显示在左下角（距底 68px），`elementFromPoint` 命中自身，**不被遮挡** | 待确认 |
| 2 | 「**为什么地图缩放不能缩放**，放大到一个城市后不能继续放大？**之前不能全球都能看的吗**」→ 原因是场景配置写死：`data/scenario-1/task-areas.json` 的 `minZoom:10 / maxZoom:14`（瓦片包其实有 z0–z14）。改 **2–18**（内联副本 + 磁盘 JSON 两份同步）。**实测**：`minZoom=2, maxZoom=18` | 待确认 |
| 3 | 「上方的工具栏，**再靠近上方一些，留大概 5 像素即可**」→ 工具条 `top: 12 → 5`（并统一了其余屏，用负偏移抵消各自浮层容器）。**实测**：工具条 y=47 = 内容区顶 42 + 5 | 待确认 |
| 4 | 「**点击选中状态，无选中状态**」→ 选中样式从"半透明蓝 + 淡边"改成**实心蓝底 + 亮蓝描边 + 发光 + 加粗**。**实测**：点【测距】前 `选择:1 / 测距:0` → 点后 `选择:0 / 测距:1` | 待确认 |
| 5 | 「上方工具栏，**点击太高了，只要工具按钮那么高就行**；点击后的提示，**重新 new 一个悬浮的提示在工具栏下方**就行，不是现在这样，很丑」→ 工具格由三行竖排改**单行**（高 52 → **35px**）；提示从工具条里**拆出来独立悬浮**在工具条正下方。**实测**：提示 y=88（工具条底 82），文字「当前工具：测距（单击落点，双击 / Enter 结束，Esc 退出）」 | 待确认 |
| 6 | 「**清屏功能，不同于全屏功能**，清屏应该**隐藏所有没有绘制在地图上的东西，按 esc 再次显示出来**」→ 地图层从 SH-03 屏内搬到 App 统一层；清屏 = 除地图外**全部 UI 隐藏**（只隐藏、不卸载，各屏画在地图上的图元不丢），**Esc 恢复**。**实测**：点后 左导航/状态条/工具条**全隐藏**、地图还在、出现「退出清屏 (Esc)」；按 Esc 全部恢复 | 待确认 |
| 7 | 「**3d/2d/3d 功能直接删除**，目前不需要」→ 工具格 10 → **9**，3D 那一格与它的图标分支一并删除 | 待确认 |
| 8 | 「显示模式：综合态势，**应该单独是一个下拉框**，而不是和功能一起」→ 宿主 `view.compose` 新增 `modes[]`（从引擎 `availableModes()` 取，**前端不自己编清单**）；界面改成**独立的一块 + 真 `<select>`**（10 档）。**实测**：`SELECT`，选项 = 综合态势/侦察展开/侦察融合/目标识别/任务规划/打击确认/实时态势/引导控制/复核态势/结果汇总 | 待确认 |
| 9 | 「**所有的标签编辑框，都删除选标签样式**」→ 标签编辑框里的「样式」二字 + 三个样式按钮（角标/卡片/引线标注）整块删掉；样式固定成角标（`BOUND_TEXT_STYLE='tag'`）。模块侧 `TEXT_STYLES` 与 `setBoundStyle()` 能力保留，只是界面不再有入口 | 2026-09-21 已改，待你复测 |
| 10 | 「**手动绘制图元后，弹出的标签框，隐藏取消按钮**；绘制图元时，不需要取消按钮」→（当时口径）弹框去掉那枚【取消】与 Esc；绘制过程中本来就没有取消按钮，只做了"绘制中要有 Esc 取消提示"（见第 11 条）。**注：这条后来被第 13 条推翻——"取消加回来，并且 esc 也加放弃绘制"** | 2026-09-21 已改（后被第 13 条推翻） |
| 11 | 「画"点"不需要 Esc；【新建】画**线 / 面**时也要出现并带 Esc 提示」→ 新增**提示态** `hintMode`（`hint-line` / `hint-area`，与绘制用的 `mode` 正交，不参与绘制判断）；点了【新建】的线/面就在**工具栏下方那行**显示「（单击落点，双击 / Enter 结束，Esc 取消）」，只出一处、不带"当前工具"前缀；**量算一字未改**；点不给提示 | 2026-09-21 已改 |
| 12 | 「**点/线/面绘制完成后，不仅弹出标签编辑框，还需要弹出经纬度编辑框，合并为一起，点击确认后绘制**」→ 新增**几何草稿**（`core/draft.ts`）：收笔/落点**不再直接落图**，先产生草稿（图上画的是预览），宿主弹**合并框**（标签 + 逐顶点经纬度），【确定】才 `commit()` 落图、【取消】/ Esc 则 `discard()` **图上不留痕**（或按快照还原） | 2026-09-21 已改 |
| 13 | 第 12 条的逐轮口径（都是你点名定的）：**先落图、可改坐标、确认才定死**（草稿态 = 图元照常画 + 编辑手柄亮着 + 框开着；确认后手柄收起、框关掉）；**每个顶点一行**（行内 经度/纬度）；**5 位小数**、**经度 ±180 / 纬度 ±90**；**框限高 + 内部滚动**；**加行 / 删行**（加行默认坐标 = 复制最后一个顶点，追加末尾；**点没有这两枚按钮**）；**改坐标 / 拖手柄 → 图上形状实时跟着变**；**顶点到下限不允许继续删（按钮灰掉）**，**【确定】仍可点、点了拦住并提示**（"面至少 3 个顶点"）；**取消加回来 + Esc = 放弃绘制**；**编辑态点击选中图元也弹这个合并框**（标签与坐标都可改，拖顶点后框里数字跟着刷新）；**放弃的两种语义**：新画的 = 撤掉不留痕、改已有的 = 还原成改之前 | 2026-09-21 已改 |
| 14 | 「**浏览态，禁用新建，编辑态才启用新建**」→ 只【新建】这一格跟着选择模式走；**【量算】不受影响，随时能用**。**实现方式记一笔**：第一版想借 `always: pickMode === 'edit'` 让规则包判灰，**走不通** —— 实测宿主 `view.compose` 对 `create` 的声明是 `enabled=true, state=available`（工具没问题，是界面状态不该用），于是 `always:false` 反而判成可用；改为新增 `MapToolSpec.disabled`（**本屏点名的禁用位**，优先于 `always` 与规则包），表现与规则包判灰一致：灰置 + `not-allowed` + 点不动 + `title` 写明"浏览态不能新建，切到「编辑」再用" | 2026-09-21 已改（第一版做错，已更正并重建前端） |
| 15 | 「在画**任务区 / 集结区**，需要两种模式：一种是用户手动点击绘制，一种是**点击一个中心点后，用户指定区域长宽，自动绘制**」→ **最终口径（2026-09-21 二次重做）**：「任务区重做，把之前的任务区变成**二级菜单**，点击后出现**任务区（手动点位）/ 任务区（自动长宽）**；集结区同上，其他刚刚加的同上」→ 工具条从"两级"扩到**三级**：一级分类（点/线/面/标绘）→ 二级条目（其中 5 条是**分组**，行尾带 ▸）→ 三级模式（手动 / 自动，出现在**最右侧一列**，与一级→二级同款）。5 组 = 任务区 / 我方集结区（手动点位 · 自动长宽）、距离环 / 方位圈（手动半径 · 自动半径）、九宫格（手动边长 · 自动每格）。**项数不变（仍是 18 条）—— 增加的是层级**。自动模式：点中心 → 合并框填尺寸 + 标注名称 → 确定画出（矩形用 4 角点的普通面；绘制阶段不问方位，朝向由第 16 条 `R` 负责） | 2026-09-21 已改（**第一版做成了"两个平级条目"，按需求方要求重做为三级**） |
| 16 | 「绘制的图元，**选中状态后支持按键选择，需要提示，按键定为 r**」（先说不做、后澄清："**因为需要添加旋转功能**"）→ **编辑态按 `R`：选中的区域逆时针转 15°**（一个方向，不需要反向），地图下方编辑浮层补提示「**按 R 逆时针旋转 15°**」。实现：模块新增 `rotateGeometry(id, deg)`（绕图元**外接框中心**整体转顶点再写回；宿主不必知道顶点存哪个字段）。**范围（需求方点名）**：只做**区域（面）**；点 / 线 / 航线 / **距离环 / 方位圈 / 九宫格**都**不旋转** | 2026-09-21 已改 |
| 17 | 「**距离环、方位圈、九宫格也需要两个模式**：指定大小模式点一个中心点，然后弹窗写半径大小与标注名称，确定画出来；九宫格写**每个格子长度**」→ 与第 15 条**同一套三级菜单**（距离环 / 方位圈 / 九宫格 三条是分组，点开是「手动半径 · 自动半径」「手动边长 · 自动每格」）。框里字段（需求方定）：距离环 / 方位圈 = **半径（km）** + 标注名称；九宫格 = **每格长度（km）** + **行** + **列** + 标注名称（默认 3×3）。刻度间隔（方位圈）**不做输入**、距离环三圈仍按 1/3、2/3、1 分。**弹窗沿用现有合并框**（同一套 确定 / 取消 / Esc） | 2026-09-21 已改（随第 15 条一起改成三级） |
| 18 | 「**中心点点击的位置，也可以手动编辑**」+「**不只是任务区**」+「同一行两格，**需要预览**，**默认宽度也能预览**」+「复用**草稿那套虚线青色**预览，预览在图上**不算正式图元**」→ 5 个自动模式（任务区 / 我方集结区 / 距离环 / 方位圈 / 九宫格）的合并框里：只读的「中心点 …」换成**经度 / 纬度两个输入框**（与顶点表同款样式与校验）；**弹框那一刻就按默认值**在图上画出**虚线青色**预览，改中心 / 改尺寸**实时跟着变**；预览**不进图元集合**（图层面板看不到、不参与导出），**取消 / Esc 即消失**。**实现**：`map-2d` 新增**形状预览**通道（`src/core/shapePreview.ts` + `DrawLayer` 的预览分支 + `index.ts` 导出，与几何草稿是两条独立通道）；宿主抽出**共用的 `SizeSpec`**（`sizeSpecOf`）驱动"预览"与"确定"，保证预览与落图同源 | 2026-09-21 已改 |

**附：落点（仅供追溯，不是需求）**
- **2026-09-21 第 15~17 条的落点**：模块 `map-2d/src/primitives/draw-api.ts`（新增 `rotateGeometry()`）、`map-2d/src/ui/DrawLayer.tsx`（`ROTATE_STEP_DEG` / 绑 `R` 键 / 编辑提示）、`map-2d/src/index.ts`（导出）；
  宿主 `apps/web/src/biz-catalog.ts`（`BizEntry.size` / `BizSizeSpec` / `SIZE_ENTRIES` 5 条 / `GROUP_KEYS` 归组）、`apps/web/src/screens/SituationScreen.tsx`（`EditorState` 新增 `size` 形态、尺寸表单、`submitSizeEditor` / `rectRing` / `drawPolygon`、`pendingSizeBiz` 中心点标记）
- map-2d 模块：`src/core/controls.ts`（`ControlSpec` / `configureControls` / `anchorStyle`）、`src/core/store.ts`（`controlLayout`）、`src/ui/CoordReadout.tsx`、`src/ui/Compass.tsx`、`src/core/commands.ts`、`src/index.ts`
- 宿主/前端：`apps/web/src/map-style.ts`（`controls` 段解析）、`MapStage.tsx`（配置驱动控件 + 渲染 `CoordReadout`）、`scenario.ts`（缩放范围）、`shell/MapTools.tsx`（工具条/选中态/提示/删 3D）、`screens/SituationScreen.tsx`（显示模式下拉框、不再自带地图）、`App.tsx`（地图层与清屏、Esc）、`flow/useSituation.ts`（`modes`）、`packages/host/src/flow.cc`（`view.compose` 补 `modes[]`）
- 配置：`data/scenario-1/map-style.json`（新增 `controls` 段）、`data/scenario-1/task-areas.json`（缩放范围）
- **2026-09-21 第 9~14 条的落点**：`apps/web/src/screens/SituationScreen.tsx`（合并框 `GeometryEditor` + 逐顶点坐标行 + 加行/删行 + 确定/取消/Esc + 浏览态禁用新建）、`apps/web/src/shell/MapTools.tsx`（`ToolModeNote` 的提示态分支）、`apps/web/src/MapStage.tsx`（草稿态不进编辑态）；
  模块侧 `map-2d`：**新增** `src/core/draft.ts`（草稿 store）、`src/ui/DrawLayer.tsx`（收笔转草稿 / 草稿预览与手柄拖拽 / 草稿态不抢键盘）、`src/primitives/draw-api.ts`（`commitDraft` 提交、按 id 读写顶点 `verticesOfId`/`setVerticesOfId`/`findGeometry`/`isVertexListKind`）、`src/core/interaction.ts`（`hintMode` / `GEOMETRY_KEYS`）、`src/index.ts`（导出）

**附：改造过程中我自己引入又修掉的两个问题（如实登记，都不是需求）**
① 清屏改造把地图层挪出 `AppShell` 之后，地图跑到左侧导航底下（x=0，应为 78）→ 修：不清屏时地图让开 `NAV_W`；
② 地图层放在 UI **之前**时，`AppShell` 的 `CONTENT_INSET` 是铺满内容区的定位盒子，**把地图上的点击全吃掉了**
（表现：量算点不出点，量算自证掉到 4/6）→ 修：地图层挪到 UI **之后**（面板靠 `z-index:20+` 仍压在地图之上）。
修完量算恢复 7/7（6111 m / 112.6°）。

---

# 变更记录

| 日期 | 改了什么 |
|---|---|
| 2026-09-18 | 新建本文档；写入 **SH-01** 的 10 条需求（均来自你 2026-09-17~18 的逐屏确认原话） |
| 2026-09-18 | 你说"启动界面完成了"→ **SH-01 标记为已确认完成**；追加第 11 条（撤下调试按钮、恢复自动进自检）；实测：打开页面后界面自己从 SH-01 走到 SH-02，全程未点任何按钮 |
| 2026-09-18 | 你说"第 2 屏确认完了"→ 写入 **SH-02 的 4 条**（左栏加宽 / 卫星链路走配置 / 自检 15~30 秒流程 / 五张卡图标）+ 新建**「全局」**一节记第 5 条（删流程徽标，作用于所有屏）；**SH-02 标记为已确认完成** |
| 2026-09-18 | 本单同步一份进仓库快照 `mission-app\docs\参考\需求确认单.md`（`参考文档\` 本身不是 git 仓，快照进仓才能被 clone 带走） |
| 2026-09-18 | 「启动界面太快了，加载总长度 30 秒左右」→ 写入 **SH-01 第 12 条**（pacingMs 400→6000，实测 30.7 秒）；同条带出的 **boot.reset 把启动页钉死**缺陷已修并登记在 SH-01 附注里 |
| 2026-09-18 | 你说"和刚才的第三屏一起提交"→ 写入 **SH-03 的 8 条**（鼠标坐标走配置 / 缩放范围 / 工具栏位置 / 选中态 / 工具条高度与提示 / 清屏+Esc / 删 3D / 显示模式下拉框）；**SH-03 如实标记为"待确认"**（你还没说过"这屏可以了"），代码与本文档一并提交 |
| 2026-09-21 | 「启动动画为 5-15 秒范围内，当前太慢了」→ 写入 **SH-01 第 13 条**（pacingMs 6000→1500，按旧口径折算约 8.2 秒）；同轮把 `App.tsx` 那段旧注释（写着 30 秒）与 `BootScreen.tsx` 注释里的"30 秒"同步成当前口径。**只改前端一处代码，宿主未动**；复测由你手动做 |
| 2026-09-21 | 「所有的标签编辑框，都删除选标签样式」+「绘制图元后标签框隐藏取消按钮」+「【新建】画线/面时要有 Esc 取消提示」→ 写入 **SH-03 第 9~11 条**（样式选择器删除、取消按钮删除、提示态 `hintMode`）；同轮改 `map-2d` 的 `core/interaction.ts` 与 `src/index.ts` |
| 2026-09-21 | 「点/线/面绘制完成后…弹出经纬度编辑框，合并为一起，**点击确认后绘制**」→ 写入 **SH-03 第 12~14 条**（合并框 + 几何草稿"确认才落图" + 逐顶点坐标/加删行/实时联动/放弃两种语义/浏览态禁用新建）。**新增 `map-2d/src/core/draft.ts`**，改了模块的 `DrawLayer` / `draw-api` / `interaction` / `index` 与宿主的 `SituationScreen` / `MapTools` / `MapStage`。静态检查：两边 `tsc --noEmit` 0 错、`vite build` 通过；**探针与起服务未跑，复测由你做** |
| 2026-09-21 | 你复测报的两个缺陷，都已修并重建前端：① **浏览态【新建】没灰** → 根因是"想借 `always` 让规则包判灰"，而宿主对 `create` 的声明是 `enabled=true/available`，于是反而判成可用 → 改为新增 `MapToolSpec.disabled`（本屏点名的禁用位）；② **区域拖拽后点确认还是原样** → 根因是框里的坐标是"打开框那一刻"的副本、拖拽只写了模块没回读，确认时反被旧坐标覆盖 → 加"图元变更 → 现读回框"（含"坐标框有焦点时不回读""确认/取消过程中不回读"两道闸门，并按"一行=一个真顶点"规范面的闭合环，避免多出一行/重复点）。③ 随后又报"**拖动确认后虚线还在**" → 根因是虚线由**选中态**画、而我只结束了**编辑会话** → 确认/取消各补 `clearSelection()` |
| 2026-09-21 | 「当前绘制的标签太难看了，字体糊，对比度低」→ 走 **A+B** 两档（A=渲染参数、B=重生成字形）：**A** `text-halo-width` 0 → **1.0**（黑描边，把白字从影像里抠出来）、底块不透明度 **0.68 → 0.85**（两处：图层缺省 + `text-layer.ts` 逐要素属性）、框边线 `.40 → .55`；**B** 字形生成器 `RADIUS` **2 → 8**、`EDGE` **0.95 → 0.80**（加粗量与旧版一致，只把 SDF 过渡带拉宽 4 倍），重生成 84 段 PBF 覆盖 `apps/web/public/fonts/MapApp/`。**重生成前置验证**：先证明当前出货字形就是 `Noto Sans SC` + `scripts/.regen-common.ps1` 的产物（`.glyphs-common` 与出货目录**逐文件 SHA256 全等、84/84**），所以只改 RADIUS 不会动字体度量、字框不会错位；**结果验证**：新旧抽样字形 SDF 直方图 **3 档 → 6 档**（边缘过渡更连续）。**注意**：字体 PBF 由宿主从磁盘实时读，**不需要重启宿主**；前端已重建。观感复测由你做 |
| 2026-09-21 | 你说「**新加的位置和之前的放在一起，不然不好点**」+「**任务区重做**：把之前的任务区变成二级菜单，点击后出现任务区（手动点位）/ 任务区（自动长宽）；集结区同上，其他刚刚加的同上」→ 把第 15/17 条的"两个平级条目"**重做成三级菜单**：`MapTools.tsx` 支持第三列（`SubmenuLeaf.items`、`leavesOf()` 三层摊平、`openMenu.sub`、`data-submenu-levels="3"`）、`biz-catalog.ts` 新增 `MODE_GROUPS` + `MANUAL_ENTRIES`（手动模式 5 条，key 加 `:manual`）与 `bizEntryOf`（避开 TDZ）、`SituationScreen.tsx` 的 `SH03_ITEMS` 按 `modeGroupOf` 生成三级。菜单仍是 18 条（增加的是层级）。静态检查：`tsc --noEmit` 0 错、`vite build` 通过（`index-3n15ArGg.js`） |
| 2026-09-21 | 你报「**新建任务区长宽，点击地图无效**」→ 根因：模块的 `MapDraw.on('click')` 是**图元命中事件**（`map-2d/src/core/primitiveEvents.ts` 里明写"空白处点击不触发"，`if (!hit) return`），而尺寸模式要点的恰恰是**空地上的中心点** → 回调永远不来。**修法**：改用**地图原始点击** `map.on('click')` 接中心点（`SituationScreen.tsx`，含"地图未就绪就等下次渲染"与卸载时 `off`）。已重建前端（`index-Bk-BLvkL.js`） |
| 2026-09-21 | 你提「**中心点点击的位置，也可以手动编辑**」+「**不只是任务区**」+「同一行两格，**需要预览**，**默认宽度也能预览**」+「复用**草稿那套虚线青色**预览，预览在图上**不算正式图元**」→ 写入 **SH-03 第 18 条**。做法：**先在 `map-2d` 开一条独立的"形状预览"通道**（`core/shapePreview.ts` 新文件 + `DrawLayer` 预览分支 + `index.ts` 导出；与几何草稿互不影响），宿主把「中心 + 尺寸」算成**一份共用规格 `SizeSpec`**（`sizeSpecOf`）—— **预览与确定都用它**，杜绝"预览一个样、落图另一个样"；框里中心点改成经纬度两格输入。静态检查：两边 `tsc --noEmit` 0 错、`vite build` 通过（`index-GcyY2mOR.js`） |
| 2026-09-21 | 你要求**排查**「用了任务区（…）后名称和之前不一样会不会出 bug」→ 结论：**两条路不会错**（规划航线是**子串**匹配「任务/集结/威胁」，改名照认；保存/打开计划按图元 `kind` + 文件里的 `text`，与名字无关）；**一条真缺陷已修**：`biz-catalog.ts` 的 `bizNameOf()` 用**前缀**匹配业务图元 id（`B:<key>:<序号>`），而键里现在带冒号（`:manual` / `:size`）→ ① 短键先命中，把「任务区（手动点位）」显示成 `任务区`；② `biz:ring:manual` 对不上 `B:ring:1` 而落空。改为**先精确一段匹配（`B:<key>:`）、再整段相等**，并把目录**按键长从长到短**排序。影响面：只影响图层面板的**兜底显示名**（正常路径是图元自身文本 → 绑定文本 → 这里） |
| 2026-09-21 | 「在画任务区，集结区，需要两种模式：一种用户手动点击绘制，一种是点击一个中心点后，用户指定区域长宽，自动绘制」+「距离环 / 方位圈 / 九宫格也要两个模式」+「选中状态后支持按键 `r`，需要提示」→ 写入 **SH-03 第 15~17 条**。做法：**每条模式各占一个菜单条目**（尺寸模式条目名带后缀），点中心点后在**合并框**里填尺寸 + 标注名称 → 确定画出。落到模块：`draw-api.ts` 新增 `rotateGeometry()`（绕外接框中心整体转顶点，逆时针，模块不写死角度）；`DrawLayer.tsx` 绑 `R` 键（**编辑态**、一次 15°、一个方向）+ 编辑浮层补提示「按 R 逆时针旋转 15°」。落到宿主：`biz-catalog.ts` 追加 5 个尺寸条目（面 +2、标绘 +3，菜单 18 → **23 项**）、`SituationScreen.tsx` 合并框新增**尺寸表单**形态（长宽 / 半径 / 每格长度 + 行 + 列 + 标注名称）。**需求方明确不做的**：点 / 线 / 航线 / 距离环 / 方位圈 / 九宫格**都不旋转**；绘制阶段**不问方位**（朝向靠 `R`）。静态检查：两边 `tsc --noEmit` 0 错、`vite build` 通过；**探针与截图未跑** |
| 2026-09-21 | 你复测后反馈"**是新的，但是还是很糊**"，并要求「**放弃该项，备注进代码去，后续修改**」→ **本轮不再改观感**，把诊断与方案**落进代码注释**（后续接手的人不必重新排查）：主备注在 **`map-2d/scripts/gen-glyphs.cs` 文件尾部**「附着：标签清晰度现状与候选改法」——含**四条根因**（①笔画太细扛不住 SDF 抗锯齿〔主因〕②字形贴图被放大采样 ③亚像素落位 ④对比度已够）、**四条候选改法及代价**（①EM 24→48〔推荐·不换字体〕②换更粗字体〔度量会变，字框要重对〕③开 `localIdeographFontFamily` 本机生成〔混排风险 + 导出无汉字〕④字号 24→28~30）、**还没实测的三件事**（对照实验、原尺寸截图、"24px 是 1:1"那句会随 EM 改动而过期）。另有指针写进 `src/render/LayerManager.ts`（`text-size` 处）与 `src/core/options.ts`（`localIdeographFontFamily` 处）；同轮**更正了两处已过期的自述**（"只造非表意字符/577 KB"与"SDF 边缘在 0.5") |
| 2026-09-21 | 你报「**编辑模式点任意图元，弹窗没有使用标注名称，而是 id 号**（`编辑面 · area:size-area-mub3ho53`）」→ 根因：编辑态点击回调取标签时**只读了 `item.text`**，而 `map-2d` 的原生文字字段**按种类不同**（`NATIVE_TEXT_FIELD`：点用 `text`，**面/线/圆/军标用 `label`**，链路/集群用 `name`）；尺寸模式画的矩形是宿主直接 `MapDraw.add('area', { label })` 落的库 → `text` 是 undefined → 标签格子空白、标题只剩 id。**修法**：按「图元自身文本（三种字段都试）→ 模块绑定文本 → 业务目录名」取（与图层面板 `nameOf()` 同一口径）；**标题改为优先显示标注名称**（例：`编辑面 · 任务区`），完整 `kind:id` 挂进 `title` 属性备查。已重建前端（`index-Ci37gkwp.js`） |
